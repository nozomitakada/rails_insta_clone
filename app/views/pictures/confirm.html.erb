<header>
    <h1 class="hed_title hed"><%= link_to 'Instagram-Clone', '/pictures' %></h1> 
</header>


<h3>以下の内容で、送信する。</h3>

<%= form_with(model: @picture, url: pictures_path, local: true) do |form| %>
  <%= form.hidden_field :image_cache %>
  <%= form.hidden_field :content %>

  <p class="confirm_img">画像:<%= image_tag(@picture.image.url) %></p>
  <p>本文:<%= @picture.content %></p>
  <%= form.submit  "登録する" %>
<% end %>

<%= form_with(model: @picture, url: new_picture_path, local: true, method: 'get') do |form| %>
  <%= form.hidden_field :image_cache %>
  <%= form.hidden_field :content %>
  <%= form.submit "もどる" , name:'back' %>
<% end %>